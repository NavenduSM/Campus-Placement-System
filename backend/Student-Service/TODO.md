# TODO: Create Sample JSON Data and URLs for Postman Testing

## Steps to Complete
- [x] Create `sample-data.json` file in the root directory with sample JSON payloads for all API endpoints (Student, StudentAcademics, StudentSpecialCriteria).
- [x] Include sample data for POST, PUT requests, and example responses for GET.
- [x] Provide a list of all API URLs for Postman testing, including base URL (http://localhost:8080).
- [x] Note special handling for StudentController (form-data for file upload, but provide JSON equivalents).
- [x] Verify the file is created and contains accurate data based on DTOs.
